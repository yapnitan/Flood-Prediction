SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Optional — enables the "Analyse a photo" AI prefill on the Report Asset Loss
# wizard. Get a key at https://console.anthropic.com/. Leave blank to hide the
# feature. NOTE: this key ships inside the app binary; fine for coursework, a
# production app would proxy the call through a server.
ANTHROPIC_API_KEY=
