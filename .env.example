SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Optional — enables the "Analyse a photo" AI prefill on the Report Asset Loss
# wizard (Google Gemini). Get a free key at https://aistudio.google.com/apikey
# (no separate Cloud project needed). Leave blank to hide the feature. NOTE:
# this key ships inside the app binary; fine for coursework, a production app
# would proxy the call through a server.
GEMINI_API_KEY=
